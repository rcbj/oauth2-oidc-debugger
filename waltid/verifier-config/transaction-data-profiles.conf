// OID4VP section 8.5 transaction data types this verifier would accept. Nothing
// in this suite sends transaction data yet — our wallet does not implement it —
// but verifier-api2 reads this file, and an empty list is a clearer statement of
// "not used here" than a missing file.
transactionDataProfiles = []
