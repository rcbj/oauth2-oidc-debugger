// The test network speaks plain HTTP, so did:web resolution must not insist on
// HTTPS. Nothing here resolves a did:web today; the setting keeps the service
// from refusing to start in an http-only environment.
enableDidWebResolverHttps = false
