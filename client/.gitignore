node_modules
# Static build output (produced by build.js / npm run build)
dist
# Transient copy of common/data.js staged during the browserify build
src/data.js
# Build stamp written by version.js --stamp (ships inside the image / dist)
public/version.json
