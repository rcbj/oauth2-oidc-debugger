# client/ — the Express frontend (port 3000) and everything in the browser

Scope: `client/src/`, `client/public/`, the bundles, the pages and their layout. Cross-cutting matters — versioning and the `{{VERSION}}` stamp, `CONFIG_FILE`, the key-material opt-out rule, how the suite is run — stay in the repo-root `CLAUDE.md`. The tests that drive these pages are in `tests/CLAUDE.md`, the edge landings they hand off to in `infra/CLAUDE.md`, and the mock issuer/verifier they talk to in `docs/mock-sts.md`.

**`/client/`** — Express frontend (port 3000). Serves static HTML/JS pages and handles the OAuth2 redirect callback at `/callback`, forwarding query params to `oauth2_oidc_2.html`.

This file holds what applies across the whole frontend: the shared modules, the landing page, and the build. Each workflow's own hard-won detail is one file away, and **each of these is worth opening before you edit the pages it names** — every one records a defect that looked like something else:

| Read | Before | Because |
|---|---|---|
| `docs/oidc-flows.md` | `oauth2_oidc_1.html`, `oauth2_oidc_2.js`, the grant dropdown | five of the six OIDC flows sent `response_type=code` for months; a test that only checks "a token came back" is worthless here |
| `docs/rfc9700.md` | `rfc9700.js`, the compliance checkbox, `client/server.js`'s headers or its `/callback` | the whole of it is behind a switch that ships CLEAR, and that contract is invisible from inside any run that turns the switch on — which is every test of it |
| `docs/scim.md` | `scim.html`, `client/src/scim*.js`, `css/scim.css` | the interesting half of that workflow is DELIBERATELY not in the page — a request built in a click handler makes a double-encoded id (a 404 that reads like a deleted user) and a wrong Digest hash (a 401 that reads like a wrong password) unfindable |
| `docs/sd-jwt-vc-issuance.md` | any `vc-issuance-*` page or `css/sd_jwt_vc.css` | the layout is measured and mutation-tested (a narrow pane *multiplies* height once populated), and Credential History logs every attempt, not just the kept ones |
| `docs/sd-jwt-vc-presentation.md` | any `vc-presentation-*` page, `sd_jwt_vp.js` | `sd_hash` covers the trailing `~`, the KB-JWT must verify against the credential's own `cnf.jwk`, and `vp_token` is keyed by DCQL query id |
| `docs/dpop.md` | `dpop.js`, `oauth_dpop.js`, `vci_wallet.js`, any protected call | the two workflows share the mechanism and **not** the state; DPoP is off by default on both, and OID4VP has none by design |
| `docs/dids.md` | `did.js`, `did-tools.html`, the DID panes on issuance step 1 | a `did:web` document read off its own origin is a circular check; the element id and the storage key are different strings, and confusing them fails silently |
| `docs/wsfed.md` | `wsfed_request.js`, `wsfed_response.js`, `wsfed_history.js`, either landing | the request page was `wsfed_tools.html` until 2026-08-09 and the rename is total; the profile's auto-POST has nowhere to land on static hosting; and the two pages each know half of what happened, which is why the history's operation labels are exported rather than typed twice |
| `docs/spnego.md` | `spnego.js`, `spnego.html`, `krb5_spnego.js`, or the return-routing on the AS and TGS pages | only the initiator's FIRST token is wrapped; the mechListMIC covers `MechTypeList` and **not** `[0] MechTypeList`; the two MICs use different keys and the asymmetry is forced by *when* each is computed; and the page routes you to two other pages and must bring you back |
| `docs/ldap.md` | `ldap.html`, `ldap.js`, `ldap_history.js`, `css/ldap.css` | the whole protocol runs in the API and not here, so the URL in the connection pane is resolved THERE; there is no "add user to group" operation in LDAP and no attribute on a user that lists their groups; and a refused operation is a completed round trip, not a failed call |
| `docs/pki.md` | `pki.html`, `x509.js`, `key_material.js`, `pki_store.js`, or the key-pair pane on **any** page | pkijs builds a Name as one multi-valued RDN and wraps an otherName in a second `[0]`, and both produce certificates that parse, display plausibly and are then refused — one of them by `openssl verify` with a message naming no extension, no name and no field |
| `docs/encryption.md` | `encryption_tools.html`, `encryption_tools.js`, `symmetric_crypto.js`, `pk_encryption.js`, `crypto_bytes.js`, `tool_panes.js` or `css/tool_panes.css` | only RSA encrypts a message directly and everything else is a KEM plus an AEAD; DSA cannot encrypt at all; and the engines are DOM-free ON PURPOSE, because a round trip through the page agrees with itself whatever the implementation does |
| `docs/webauthn.md` | any `webauthn*` page, `cbor.js`, `cose.js`, `webauthn.js`, or the browser extension | the RP ID is the calling origin and that refusal *is* the phishing resistance; the decoder is checked against the browser's own `getPublicKey()`; and an ECDSA signature arrives DER-encoded while Web Crypto wants raw `r‖s`, which fails by returning `false` rather than throwing |

## Shared client modules

Several page bundles share modules rather than duplicating behaviour:

| Module | Shared by | What it holds |
|---|---|---|
| `metadata_client.js` | `oauth2_oidc_1.js`, `sd_jwt_vc_issuance_*.js` | fetching/tabulating a metadata document, its provenance note, base64url + Web Crypto JWS verification, the `signed_metadata` verdict, and **where an issuer's metadata lives** (`wellKnownCandidates`/`fetchWellKnown`: RFC 8414 inserts the well-known before the issuer's path, OIDC Discovery appends it — both are tried, insertion first) |
| `op_metadata.js` | `oauth2_oidc_1.js`, `oauth2_oidc_2.js`, `vc_issuance_1.js` | the OpenID Provider metadata members (and, separately, the RFC 8414-only ones) with their defaults and `-->not defined<--` notes |
| `vci_metadata.js` | `sd_jwt_vc_issuance_*.js` | the OID4VCI credential issuer metadata members (element ids and storage keys prefixed `vci_`) |
| `sd_jwt_vc.js` | `sd_jwt_vc_issuance_*.js`, `oauth2_oidc_1.js`, `oauth2_oidc_2.js` | the issuance workflow's storage keys and hand-off flag, plus SD-JWT parsing and digest computation |
| `sd_jwt_vp.js` | `sd_jwt_vc_presentation_*.js` | the PRESENTATION workflow's storage keys and flows, the OID4VP request parameters and DCQL helpers (`requestedClaims`, `requiresKeyBinding`), and the artifact itself: `presentedPrefix`, `sdHash`, `signKbJwt` and `buildPresentation`, which assemble the SD-JWT+KB. It reuses `sd_jwt_vc.js` for parsing and digests, because a presentation is the same credential with a different last element |
| `vci_wallet.js` | `vc_issuance_2.js`, `vc_issuance_4.js` | the wallet's half of a **Credential Request**, with no DOM in it: holder key pairs, the `c_nonce`, the `openid4vci-proof+jwt` proof of possession, the request body, the text of an assembled HTTP call, and reading a Credential Response (JSON or the section 10 JWE). Extracted because step 4 refreshes a credential by making *the same call* step 2 makes — on the wire a refresh is not a different request, so it must not be a second implementation |
| `vci_claims.js` | `vc_issuance_1.js`, `vc_issuance_2.js` | **which claims the wallet asks the issuer to put in the credential**: the rows read out of `credential_configurations_supported[<id>].claims`, the selection (stored as the claims EXCLUDED, so a newly advertised claim is checked rather than silently dropped), and the `claims` member it becomes. No DOM. It exists because the member belongs to **`authorization_details`** (OID4VCI section 5.1.1) and NOT to the Credential Request, so two calls carry it — step 1's authorization request and, for the pre-authorized flow that has no authorization request, step 2's Token Request — and they must agree about what "everything selected" sends, which is nothing. Since 2026-08-19 step 1 also **defaults the request mechanism to `authorization_details`** when the chosen configuration advertises claims, because a scope request has nowhere to carry the selection and two readers in a row took the resulting silence for the issuer ignoring them. See `docs/sd-jwt-vc-issuance.md` |
| `dpop.js` | `vci_wallet.js`, `vc_issuance_2.js`, `vc_issuance_4.js`, `oauth2_oidc_2.js` | **DPoP** (RFC 9449): the key pair, the RFC 7638 JWK Thumbprint that becomes `cnf.jkt`, `htu`/`ath`/`jti`, the `dpop+jwt` proof, and recognising the server's nonce request in either of its two shapes. No DOM and no storage, which is what lets `tests/dpop_vectors.js` check it against the RFCs' own vectors; the wallet's memory of it is in `sd_jwt_vc.js` and the wire is `vci_wallet.js`'s `fetchProtected()` |
| `edge_landing.js` | `saml_response.js`, `wsfed_response.js` | the **client half** of the static deployments' edge-landing hand-off: the sessionStorage key names, the `?posted=` marker and `takeHandoff()` (read once, then delete). Its counterpart is `infra/edge/edge_common.js`'s `CONTRACTS`, which ships to AWS by Terraform while this ships in the bundle — they cannot import each other, so `tests/edge_landing_contract.js` loads both and fails on drift |
| `op_history.js` | `saml_history.js`, `wstrust_history.js`, `wsfed_history.js`, `kerberos_history.js`, `ldap_history.js` | `pki_history.js` | the Operations History log. `classPrefix` and `resultClasses` are options because the fourth sibling's pages do not link `css/saml_common.css`, and a `saml-*` class on a page that never loaded that sheet is exactly what `checkStylesheetsLoaded()` in `tests/navigation.js` fails on |
| `kerberos_history.js` | `kerberos_panes.js` and all five `kerberos*` bundles | the Kerberos log, fourth sibling over `op_history.js` — and the only one whose rows are closed from somewhere else. `panes.status()` calls `settle()`, because these handlers have too many early returns to close a row at each of them and a row nobody closes stays `Sent`, which in this pane *means* the KDC never answered. `begin()` names the status line that will report the operation; the open rows are keyed by that id, since the delegation page has four lines and can have four in flight. See `docs/kerberos.md` |
| `kerberos_keys.js` | the **`spnego.js`** bundle **and `kerberos_decoder.js`** — and no other page, see below | the three routes to a decryption key (raw hex + etype, a principal's password *and* salt, a keytab) and the button that re-reads every message pane with them. **No exchange page asks for a key any more.** A shared *Decryption keys* pane sat at the foot of the TGS, AP, delegation and SPNEGO pages for one day in August 2026 and was removed: what it asked for was already in the browser. The KDC repeats a ticket's flags, session key, principals and times in the reply's enc-part, every page stores exactly those when it caches a ticket, and `kerberos_panes.js` now hands that report to **every** message it describes (`ticketReports()` → `describe({reported})` → `reportedFor()` in `krb5_describe.js`), matched on the ticket's own DER and never on its principal. So a TGT nested inside a TGS-REQ's `PA-TGS-REQ` and a service ticket inside a SPNEGO `mechToken` show their contents with nothing typed; `heldSessionKeys()` adds the cache's own session keys, which opens Authenticators, AP-REPs and KRB-CREDs the same way. **The AS page has no key pane and must not grow one** (the key would be `krbtgt`'s), and neither may the TGS, AP or delegation pages. **SPNEGO keeps its fields, inside its Ticket pane** where they were before that pane existed: the ticket there is a *service* ticket whose keytab a reader plausibly holds, and the one thing a key still buys is the **PAC**, which lives only in the ciphertext. It calls `mount()`, which registers `collect` with `kerberos_panes.js` (`setExtraKeys`). **It stores nothing, in either store** — see `tests/kerberos_as_page.js` and `tests/kerberos_spnego_page.js`. The decoder page uses `collectFrom()` with its own id map (`krb_key_hex` / `krb_password` / …) and never `mount()`, because it passes its keys to the describer itself and must register nothing. See `docs/kerberos.md` |
| `kerberos_tickets.js` | all six Kerberos bundles (the five `kerberos*` ones and `spnego.js`) | the **Ticket Cache & History** pane (`public/partials/krb_tickets.html`), which is a *credential store* rather than a log — each row keeps a ticket and its session key so any of them can be put back in use, which is why it obeys `krb_save_ccache` and the Operations History pane beside it does not. One module because what differs per page is only the **slot** that page holds: `mount({slots})` decides which rows offer *Make active*, and `kerberos_panes.js`'s `TICKET_SLOTS` decides where an activated one is written — from the ticket's own kind, never from a caller. The decoder mounts it `readOnly`, because that page must write nothing at all. See `docs/kerberos.md` |
| `wsfed_history.js` | `wsfed_request.js`, `wsfed_response.js` | the WS-Federation log, third sibling over `op_history.js`. **It also exports the two operation labels** (`OP_SIGN_IN`, `OP_SIGN_OUT`), because the request page writes a pending entry under one and the response page resolves it by matching that same string — spell it in two places and the row never closes, which reads as an IdP that never answered |
| `xmldsig.js` | the SAML and WS-Trust pages | in-browser XML Signature / XML Encryption |
| `css/saml_common.css` | the SAML, WS-Trust **and WS-Federation** pages | the shared `saml-*` look: panes, fields, tabs, buttons, notices, the collapse toggle. Every page in that family links it, and only the extras go in a page-specific sheet beside it (`saml_tools.css`, `wstrust_tools.css`). It was itself called `saml_tools.css` before the 2026-07-26 rename, which is how the WS-Federation pages ended up linking the small page-specific sheet after a merge and rendering completely unstyled — the link resolved, so nothing 404'd. `checkStylesheetsLoaded()` in `tests/navigation.js` now catches that: any class prefixed `saml-` / `wst-` / `wsfed-` / `vc-` / `vp-` that a page uses must be defined in one of the stylesheets that page actually loaded |
| `key_material.js` | `jwt_tools.js`, `pki.js`, `digital_signature.js`, `encryption_tools.js` | key pairs and the keystore formats: the algorithm table, generation, the PEM↔JWK conversion behind every pane's format toggle, PBES2-encrypted PKCS#8, PKCS#12 and the PBES2 JWE that password-protects a JWK set. **It is the bottom third of `jwt_tools.js`, moved** — that page is a caller now and is 340 lines shorter — because the PKI page needs the same pane and these are wire formats other people's tools have to read, so two readings of them can agree with each other and both be wrong. It has **no DOM**: `exportKeyPair()` RETURNS the files rather than downloading them, which is the whole reason `tests/pki_key_formats.js` can produce all 49 cells of the matrix in node and hand them to OpenSSL. Certificates are deliberately NOT here (`x509.js` requires this and not the other way round), so `buildPkcs12()` takes the certificates it is to wrap — the PKI page passes a real chain and the other three mint a throwaway through **`x509.selfSignedCertPem()`**, which is itself shared for the same reason. **The Digital Signature page had a second, node-forge implementation of this whole matrix until 2026-08-21** — PEM, DER, JWK and PKCS#12, written again — and it is gone; that page also generated its RSA key as PKCS#1, which is what a shared matrix immediately refused with a bare Web Crypto `DataError` |
| `x509.js` | `pki.js`, `jwt_tools.js` (the self-signed certificate its PKCS#12 and *View certificate* need), **`saml_cert.js`** (the shared certificate-details page, which parsed with node-forge until 2026-08-18 and therefore refused every EC and Ed25519 certificate with `Cannot read public key. OID is not RSA.` — see `docs/pki.md`) | X.509 certificates: the profiles, every X.509v3 extension including its critical flag, issuing, describing and chain checks. Also no DOM, so `tests/pki_x509.js` drives ~240 certificates through it in node and asserts with **OpenSSL** — which is the only kind of check that catches the five defects `docs/pki.md` records, each of which produced bytes that parse perfectly and are refused by something else. It is where **Ed25519 is signed by hand**, pkijs being unable to import an Ed25519 public key or sign with one; giving jwt_tools this module is how that page gained Ed25519 PKCS#12 by deleting code |
| `pki_store.js` | `pki.js` | the PKI page's keys and certificates, kept so a certificate authority is still usable tomorrow — which is the point of a root. The private-key opt-out is enforced in ONE write path, and it governs the private half **only**: the certificates are public documents, and keeping them leaves the trust anchors and the chain usable when the keys are gone |
| `jwk_pem.js` | `jwks.js` | a JWK public key as a SubjectPublicKeyInfo **PEM** (RSA, and EC on P-256/384/521), which is the "PEM Format" box on the JWKS page. It is sixty lines of DER rather than the `jwk-to-pem` package because that package builds its EC point through **`elliptic`**, and browserify then ships `elliptic` to the browser — see *Keeping `elliptic` out of the bundles* below. A `d` member is ignored (these are published keys), and an unsupported `kty` throws so the page can mark **one** key unrenderable without losing the table |
| `jose_jwe.js` | `jwt_tools.js`, the OID4VCI issuance panes | in-browser **JWE** (RFC 7516/7518): RSA-OAEP(-256), ECDH-ES direct and +A*KW, A*GCM, the Concat KDF, flexible key input (CryptoKey / JWK object / JWK text / PEM), and a **Web Crypto capability probe** — Chrome rejects AES-192, so options needing it are marked unusable rather than failing with an OperationError. Extracted from `jwt_tools.js` because OID4VCI section 10 has both sides encrypting, and the KDF must exist once. Tested directly by `tests/jose_jwe_encryption.js` against an independent reading of RFC 7518 section 4.6 |
| `crypto_bytes.js` | `jose_jwe.js`, `key_material.js`, `symmetric_crypto.js`, `pk_encryption.js`, `digital_signature.js`, `encryption_tools.js` | bytes, base64, **base64url**, hex, PEM framing, a constant-time compare, a random buffer. Three files each had a copy of most of it, and a copy of a conversion is a copy of a DECISION: base64 and base64url differ by two characters and a padding rule, `atob` throws on the whitespace a textarea supplies for free, and a PEM parser that keeps the header line produces bytes nothing notices are wrong until somebody else's tool reads them. One behavioural change came with the merge and it is asserted: `hexToBytes()` **refuses** a non-hex character or an odd digit count instead of reading it as zero, which used to mean encrypting under a key that was not the one on the screen |
| `symmetric_crypto.js` | `encryption_tools.js`, `digital_signature.js`, `tests/crypto_engines.js` | the block and stream ciphers (AES in six modes at three key sizes, 3DES two-key and three-key, DES, ChaCha20) and the MAC constructions over them (AES-CMAC, AES-CBC-MAC, AES-GMAC, Poly1305, SipHash-2-4). The MACs were inside `digital_signature.js` and **Poly1305 is what forced the move** — ChaCha20-Poly1305 needs the same RFC 8439 section 2.5 implementation, and two readings of that section can agree with each other and be wrong together. node-forge throughout and **not** `crypto.subtle`, deliberately: Web Crypto has GCM, CBC and CTR and nothing else, and Chrome refuses every AES-192 operation. Note forge will not do two-key 3DES — `expandDesKey()` builds `K1 K2 K1`, which is what two-key 3DES IS |
| `pk_encryption.js` | `encryption_tools.js`, `tests/crypto_engines.js` | public-key **encryption**: RSA (OAEP and PKCS#1 v1.5, direct and hybrid), ECIES over five curves, ML-KEM (FIPS 203) alone or hybridised with X25519, and the finite-field family — ElGamal and DHIES over RFC 3526's MODP groups, which is what a DSA-shaped key's group can do, since **DSA itself cannot encrypt**. Only RSA takes a message directly; everything else agrees a key and the message is encrypted symmetrically under it, which is what ECIES, HPKE, JWE and TLS all do. No DOM, so `tests/crypto_engines.js` drives it in node against the RFCs' vectors and against OpenSSL |
| `tool_panes.js` | `digital_signature.js`, `encryption_tools.js` | the pane grid's BEHAVIOUR — the DOM accessors, collapse/expand, the clipboard (with the `execCommand` fallback every containerized run takes, since `navigator.clipboard` is undefined outside a secure context), the download, and the `?from=` return link, which is a **whitelist rather than a redirect**. Its stylesheet half is `css/tool_panes.css`, renamed from `digital_signature.css` when the second page started linking it; the `ds-` class prefix is deliberately unchanged |
| `did.js` | `did_tools.js`, `vc_issuance_1.js` (the pane), `vc_issuance_3.js` (issuer verification), `vc_presentation_2.js` (the ldp_vc key) | **DIDs** (W3C DID Core 1.0): `did:jwk`, `did:key` and `did:web`, all resolving to the same document shape so one table renders any of them; reading a document (`assertionKeys`, `keyForKid`, `assertionJwks`); `resolveVerificationMethod`, which handles a proof naming its key by DID URL *or* by https URL; and **DIF Well Known DID Configuration** (`verifyDomainLinkage` / `verifyOriginLinkage`). Tested by `tests/did_document.js` |
| `cbor.js` | `cose.js`, `webauthn.js` | a decode-only, bounded CBOR reader (RFC 8949). Maps decode to a **`Map`**, because COSE is keyed by integers including negative ones and collapsing those to object keys makes `1` and `"1"` the same thing. Indefinite lengths are refused: CTAP2's canonical CBOR forbids them, so one in an attestation object is a finding rather than something to accept quietly. `decodeFirst()` exists because the credential public key sits mid-buffer inside the authenticator data with the extension data behind it |
| `cose.js` | `webauthn.js`, `webauthn_panes.js` | COSE_Key → JWK → SPKI PEM, **reusing `jwk_pem.js`** rather than re-encoding DER. A key carrying private material is refused rather than stripped. The negative labels are read per key type, because `-1` is the curve for an EC2 key and the modulus for an RSA one |
| `bbs.js` | `bbs2023.js` (so `vc_presentation_2.js`), `digital_signature.js`, `tests/bbs_crypto.js` | **BBS over BLS12-381** (draft-irtf-cfrg-bbs-signatures): both ciphersuites, KeyGen, Sign/Verify over a message *list*, and ProofGen/ProofVerify — the derived, unlinkable proof that is the whole point of the scheme. It is written out rather than installed because `@digitalbazaar/bbs-signatures` is ESM-only and browserify cannot consume it; that accident is what gives `tests/bbs_crypto.js` an independent implementation to check ours against. **A suite is an optional LAST argument on every operation, defaulting to `BLS12-381-SHA-256`** — which is the suite `bbs-2023` uses, so the VC workflow's calls did not change when the Digital Signature page needed `BLS12-381-SHAKE-256`, and the byte-for-byte cross-check still means what it meant. The two suites differ in more than a hash name (expand_message_xmd/SHA-256 vs expand_message_xof/SHAKE-256, and each has its **own** fixed `P1`), and getting one of those wrong produces signatures that verify perfectly against your own verifier and against nobody else's — which is why the draft's own vectors are vendored as `tests/bbs_vectors.json` and asserted on every run. Note `keyGen`'s default DST is built from the **bare** ciphersuite id while every other DST here uses the API id |
| `webauthn.js` | the two WebAuthn pages, `webauthn_panes.js`, `tests/webauthn_decode.js` | clientData, authenticator data, attestation statements, and assertion verification reporting every check **by name** rather than one boolean. No DOM in it, which is what lets the node test drive it against real ceremonies with no browser |
| `webauthn_panes.js` | `webauthn_analyzer.js`, `webauthn_lab.js` | the DOM half: the decode panes both pages render. They differ in where the bytes came from and nothing else, so a flags table that disagreed between them would be a bug visible only by comparing the pages. **No `innerHTML` anywhere** — everything here arrived from an authenticator or a paste box |
| `scim_client.js` | `scim.js`, `tests/scim_engine.js`, `tests/scim_protocol.js` | the SCIM endpoint catalogue (the single source for the method, path, body shape, query parameters, scope and labels of all twenty-two operations), the seven RFC 7644 section 2 authentication schemes, HTTP Digest for all three registered algorithms (**none of them Web Crypto**, which has neither MD5 nor SHA-512/256), HOBA's length-prefixed blob, the seeded RFC 7643 generator, and reading an answer |
| `scim_scenarios.js` | `scim.js`, `tests/scim_engine.js` | the twelve named scenarios and the random one, references (`{ref, field}`, resolved just before sending — a plan with a function in it cannot be shown to a person or compared by a test), and `judge()`, which is what makes a 409 on a duplicate `userName` a **pass** |

## The landing page

`client/public/index.html` is the protocol chooser — one card per protocol, styled by `css/landing.css`. **Every card has to be visible without scrolling**, which is the one thing this page must get right: it exists to be a choice, and a choice you have to scroll to find is one you do not know you have. That constraint fixes every number in that file. **Fourteen cards** now sit in a **7 + 7** grid inside bootstrap's 940px container, ending ~597px down against a 625px headless viewport and the 640px budget `landingFitsOnOneScreen()` enforces. The thirteenth (LDAP) was the expensive one that forced seven columns; the **fourteenth (SCIM) was free**, because seven across turns 7+6 into a filled 7+7 — the same two rows and therefore the same height. **The fifteenth is the next expensive one**: 7+7+1 puts a third row below the fold, so it will need eight columns at `min <= 108.75` (`8*108 + 7*10 = 934 <= 940` at the current 10px gap), and eight columns take another ~17px off every card so every description wraps again.

**The column count has had to grow four times, always for the same reason**: two columns put the last of six cards below the fold, three would have done it to the seventh, five to the ninth, six to the eleventh. The minimum width is arithmetic rather than taste, and it is the WIDEST value that works — *n* columns fit when `n*min + (n-1)*gap <= 940`, so six need `min <= 148.3` at a 10px gap. **Anything larger fails silently**: the grid simply stays at the previous count and the only symptom is the height. The eleventh card also cost an icon (40px down to 34px), a line-height and some padding — an icon costs its height once per ROW, so trimming it buys back twice what it gives up, which is why it is reached for before anybody's product copy is shortened to fit a layout. Descriptions are still kept short and roughly **equal in length**, because a grid row is as tall as its tallest card. Measure with the test at each step rather than estimating, and remember the containerized run and a host run differ by a few pixels: fonts-liberation's metrics are not a host's Arial's.

**Three cards point into the OAuth2 / OIDC pages**, because two of those protocols are panes rather than pages: Dynamic Client Registration links to `/oauth2_oidc_1.html#dcr_fieldset` (the fragment opens the page at its pane), and Token Exchange links to plain `/oauth2_oidc_1.html` — its pane is on `oauth2_oidc_2.html`, since an exchange needs a subject token first, so there is nothing on `oauth2_oidc_1.html` to scroll to. Two cards therefore share one href, which is why `tests/navigation.js` locates those cards **by title** (`cardByTitle()`) rather than by href; the CSS href locator matches both.

**PKI is the one card whose page half-works without the api, and it ships anyway.** The certificate authority itself is Web Crypto and pkijs in the browser; only the TLS test at the foot of `pki.html` needs `POST /tls/connect`, and the page says so itself with the `#pki_backend_notice` banner. So it is an ordinary card with no `data-not-on-static` marker — the test below fails a card that carries the marker without its page being in `EXCLUDED_PAGES`, and greying a workflow that mostly works is the wrong trade. That is what separates it from the three cards that follow.

**What is greyed instead is the one PANE, and it is greyed the way the cards are: switched off, not merely marked.** `disableTlsPane()` in `pki.js` runs from `onload()` when `appconfig.backendAvailable === false`, adds `pki-pane-disabled` to `#pane_tls` (the rules are at the foot of `css/pki.css`), sets `disabled` on **every** control in `#pane_tls_body`, and shows `#pki_tls_unavailable`. Both halves are needed for the reason a dead card is an `<a>` with no `href` rather than one that merely looks grey: a pane that only *looks* dead still submits on a Return keypress in a text field, and `runTlsTest()`'s own refusal is then the first thing that says anything. The legend is deliberately untouched, so the pane still collapses and expands. Two notes on the CSS: it greys with **colours rather than `opacity`**, because `opacity` makes a stacking context and the notice — the one line in there that has to be read — could then not be brought back to full contrast by any rule of its own; and `show()` in `pki.js` had to start toggling the `saml-hidden` class as well as the inline `display`, because `#pki_backend_notice` is hidden by that class in the markup, so for as long as the banner existed the static build asked for it and the class kept it off the page. `tests/pki_page.js` asserts all three properties separately, and reads `getComputedStyle` rather than the inline style — the check that read the inline style is exactly what agreed with the caller instead of with the user. See `docs/pki.md`.

**Two cards are dead on the static deployments and live everywhere else.** Kerberos needs the api's port-88 relay, so `client/static_site.js` drops all six of its pages from `dist/` — SPNEGO included, because the ticket it carries still comes from a KDC — and `client/build.js` rewrites both cards: `href` stripped, `landing-card-disabled` added, `aria-disabled="true"`. It stays an `<a>` — with no `href` it is neither focusable nor clickable, and every selector that reads this page, here and in `tests/navigation.js`, is written against `a.landing-card`; a `<span>` would drop it out of the geometry and accent-colour checks, which is exactly when a card silently stops fitting. The card is keyed off a `data-not-on-static` attribute in the markup and carries a **second** description (`landing-card-unavailable`, `display:none` everywhere else) saying why. The two descriptions **swap**, they do not stack: a row is as tall as its tallest card, so the replacement text is written no longer than the one it replaces — measured at 580px of the 640px budget either way. Note also that the disabled icon rule needs a doubled class (`.landing-card.landing-card-disabled .landing-icon`), because the `:nth-child(N)` accent rules above outrank a two-class selector however far down the file it sits; without it the card greys and its icon stays brown. `tests/static_site_exclusions.js` holds all of this, and `landingFitsOnOneScreen()` still measures the card because it is still an `<a>`.

**SCIM is the second card whose page works without the api, and like PKI it ships as an ordinary link.** RFC 7644 is plain HTTPS with a JSON body, so `scim.html` calls a SCIM server directly from the browser; what a static build loses is the api call path, and with it the ability to reach a server that sends no CORS headers or one with a self-signed certificate. The page says that itself in its Connection pane (`refreshCallPathControls()` disables the backend radio and prints the reason) rather than the card being greyed — same trade PKI makes, and the reason it carries no `data-not-on-static` marker and is not in `client/static_site.js`. Note the marker and the exclusion list are checked against each other: `tests/static_site_exclusions.js` fails a card that carries the marker without its page being in `EXCLUDED_PAGES`, so a SCIM card marked "not on static" would fail there rather than quietly greying a workflow that works.

**Three cards are dead on the static deployments and live everywhere else.** Kerberos needs the api's port-88 relay, so `client/static_site.js` drops all six of its pages from `dist/` — SPNEGO included, because the ticket it carries still comes from a KDC — and LDAP goes with them for the same underlying reason and with none of SPNEGO's ambiguity: RFC 4511 is BER over a TCP socket, so every button on `ldap.html` is a call to the api and that page has no offline half at all. `client/build.js` rewrites all three cards: `href` stripped, `landing-card-disabled` added, `aria-disabled="true"`. It stays an `<a>` — with no `href` it is neither focusable nor clickable, and every selector that reads this page, here and in `tests/navigation.js`, is written against `a.landing-card`; a `<span>` would drop it out of the geometry and accent-colour checks, which is exactly when a card silently stops fitting. The card is keyed off a `data-not-on-static` attribute in the markup and carries a **second** description (`landing-card-unavailable`, `display:none` everywhere else) saying why. The two descriptions **swap**, they do not stack: a row is as tall as its tallest card, so the replacement text is written no longer than the one it replaces — measured at 580px of the 640px budget either way. Note also that the disabled icon rule needs a doubled class (`.landing-card.landing-card-disabled .landing-icon`), because the `:nth-child(N)` accent rules above outrank a two-class selector however far down the file it sits; without it the card greys and its icon stays brown. `tests/static_site_exclusions.js` holds all of this, and `landingFitsOnOneScreen()` still measures the card because it is still an `<a>`. Note the page's own stylesheet has to go in `EXCLUDED_ASSETS` beside the page — `css/kerberos.css` and `css/ldap.css` are both there — or the static build ships a sheet nothing links.

**The header's brand logo goes to this page, from everywhere.** It used to be workflow-aware — a script in `partials/header.html` rewrote its `href` off `window.location.pathname`, so the logo meant "the home of the workflow you are in" and each family got its own target. With ten workflows that made the one control every page shares mean ten different things, and it left no single click back to the chooser from a workflow's first page (where the logo pointed at the page you were already on). It is now a plain `href="/index.html"` with no script, which is also what the "Home" nav link beside it does.

`tests/navigation.js` clicks **every** card and checks the page it lands on — its footer build number and its stylesheets. That matters beyond navigation: a protocol's own suite may be gated (the WS-Federation one needs its Keycloak side-car and is skipped without it), so for those pages this is the only place they are loaded on every run.

Adding a protocol therefore means three things here, and `landingFitsOnOneScreen()` in `tests/navigation.js` fails if any is missed: the cards must still fit (checked against the viewport *and* against a 640px budget, because headless Chrome has no toolbar and so its viewport is more generous than a real browser's), the new card needs its own `:nth-child(N) .landing-icon` accent rule or its icon silently falls back to the text colour, and no card may end up nested inside another — an unclosed `</a>` is invisible in a screenshot, since the browser recovers from it.

## The OAuth2 / OIDC pages were renamed

**`debugger.html` and `debugger2.html` are now `oauth2_oidc_1.html` and `oauth2_oidc_2.html`.** They were the only pages still carrying the name the whole project had when OAuth2 and OIDC were all it did; every workflow added since is `<protocol>_<step>`, and the two originals now read the same way. The rename is total and nothing answers on the old URLs: the pages, their bundles (`client/src/oauth2_oidc_1.js` / `_2.js`), `debugger2.css` (now `oauth2_oidc_2.css`), the `BUNDLES` entries in `client/build.js`, both sets of `browserify` lines in `client/Dockerfile` — the plain build's and the coverage build's `for entry in "name:standalone"` list, which is a *second* place and is only exercised by `./run-coverage.sh` — the `/callback` target in `client/server.js` and in `build.js`'s static callback shim, the landing cards, and the tests.

**The browser globals went with them**, which is the part that reaches every inline handler: `--standalone debug` is now `--standalone oauth2_oidc_1` and `debugger2` is `oauth2_oidc_2`, so the ~85 `onclick`/`onchange` attributes on the two pages call `oauth2_oidc_1.foo()` / `oauth2_oidc_2.foo()`, as do the `driver.executeScript` probes in `tests/oauth2_metadata_rfc8414.js`. That also fixed something that had never worked: three links at the top of `debugger.html` called `debugger.clickLink()`, and `debugger` is a **reserved word**, so those handlers were a `SyntaxError` in every browser since the page was written. The old global was `debug`, so the name was wrong twice over.

**`css/debugger.css` keeps its name on purpose.** Despite the name it is not this workflow's stylesheet — it is the shared `.dbg-*` pane chrome (the pane borders, the collapse legends, the expand-all toggle) that **21 pages** link, Kerberos and the VC workflows included. Renaming it to `oauth2_oidc_1.css` would have said something false about it; renaming it to what it actually is would have touched every one of those pages for no behavioural gain, so it was left alone and this paragraph is the answer to "why is that one still called that".

**The Configuration Parameters pane sizes its fields to its column, and the class that does it is `.dbg-config-table`.** Bootstrap gives every text input, select and textarea a fixed `width: 206px` (`input, textarea, .uneditable-input`), which outranks the `size="60"` several of those fields carry and takes no notice of the box it is in. That is harmless on `oauth2_oidc_1.html`, where the pane is one of two flex columns and about 854px wide at 1366, and wrong on `oauth2_oidc_2.html`, where the same table sits in one of THREE columns at about 419px: every field's right edge landed on the pane's border and the four boolean `select`s 25px past it, growing to 136px as the window narrowed, because the pane shrinks and a fixed field does not. So the form table carries `.dbg-config-table` and `debugger.css` gives its fields `width: 100%` with `box-sizing: border-box`, leaving checkboxes, radios and buttons intrinsic — those are as wide as they look, not as wide as their content. Auto layout is kept rather than the `table-layout: fixed` the metadata table beside it needs (that one renders a document we do not control), because here the labels are known and the label column should still take what the longest of them needs; the `width: 45%` on the first column is a preference a browser honours when the labels allow. `tests/oauth2_config_pane_layout.js` holds both pages to it at seven viewport widths, and asserts the field-to-cell width as well as the geometry — an 854px pane hides a 220px field perfectly well, so on page 1 the geometry alone can never see this coming back.

**The token exchange pane has two tabs, and the second one shows an exchange the page mostly cannot see.** *Parameters* is the form that has always been there; *HTTP* is the Token Request and its response as they actually went — method, URL, headers and body each way, and the elapsed time. Four things about it are load-bearing. **The default tab is never changed by code**: a successful call collapses this whole pane (`$("#token_fieldset").hide()`), so switching tabs from a response handler would rearrange a pane nobody is looking at and hand the next reader who expands it something other than the form they went there for — the tab's own LABEL carries the status instead (`HTTP · 200`), which is what a reader sees the moment they open it. **The bytes come from two different places**: a browser-direct call is recorded here from the jqXHR, where the browser withholds the headers it adds and CORS withholds most of the ones that come back — and the pane says so rather than presenting a partial list as a whole one — while the **proxied** call, which is this pane's default, is made by the api and can only be reported by the api, which returns it under `http_exchange` when the request asks with `http_trace: true` (see `api/CLAUDE.md`). When no trace comes back the pane falls back to the browser's own call *to the api* and names that too; a fallback that looked like the real thing would be a debugger showing the wrong URL with a straight face. **Nothing about it is drawn as markup** — `renderTokenHttpExchange()` builds text nodes, because a response body is somebody else's bytes and concatenating those into `.html()` is the `js/xss-through-dom` defect this same pane already carries a long comment about. And **nothing about it is stored**: the request repeats the client secret and, on the password grant, a password. The error pane is handed a copy with the trace taken back out (`tokenErrorWithoutTrace()`), or its five-row textarea would open on sixteen kilobytes of transport. `tests/token_http_exchange.js` holds all of it, geometry included.

**The same exchange is on the Token Endpoint Results pane too, and that is deliberate rather than a duplicate.** The exchange is *composed* on the request form, which is why the form has a tab for it — but a successful call collapses that form, so the pane a reader is actually looking at afterwards is the results one, and "what did the wire look like" is a question asked about a result. It carries a **Tokens** tab (the default: whoever just called the token endpoint came for the token) and an **HTTP** tab showing the identical view, with the identical label. What makes it one feature rather than two is that there is one view and one renderer: `renderTokenHttpExchange()` stores the view and draws it into every host in `HTTP_EXCHANGE_PANES` that exists, and `tests/token_http_exchange.js` asserts the two panels' text is character-for-character the same so that a second implementation cannot quietly grow here. Two things are worth knowing before touching it. **That pane is not in the page** — `oauth2_oidc_2.js` builds it as a STRING in four branches and drops it into `#token_endpoint_result`, one of which emits a bare `<fieldset>` with no `.dbg-pane` around it — so the tab is not markup but `attachHttpTabToTokenResults()`, which wraps whatever was just built (the tokens become the first panel) and is idempotent because the pane is rebuilt after every call. Its buttons are wired with `addEventListener` rather than an inline `onclick`: the strip is created BY the bundle, so the handler is in scope by definition. And **the empty state differs from the form's on purpose**: on the form, no exchange means none has been sent yet; on the results pane it usually means the tokens on screen came back from `localStorage` on the return from `token_detail.html` (`?redirectFromTokenDetail=true`, the only caller of `recreateTokenDisplay()`), where the exchange was never kept because a Token Request carries the client secret. Saying "nothing has been sent yet" over a pane full of tokens would be a lie, and an empty panel would read as a broken tab; both states are asserted.

## Frontend Build

Client-side JavaScript lives in `/client/src/` and is compiled into `/client/public/js/` using **browserify** with the **envify** transform (substitutes `process.env.*` at build time). Each feature page has its own standalone bundle:

| Source | Bundle | Page |
|---|---|---|
| `oauth2_oidc_1.js` | `oauth2_oidc_1.js` | Authorization/Implicit initiation |
| `oauth2_oidc_2.js` | `oauth2_oidc_2.js` | Token exchange + results |
| `token_detail.js` | `token_detail.js` | JWT inspection/validation |
| `introspection.js` | `introspection.js` | Token introspection |
| `userinfo.js` | `userinfo.js` | Userinfo endpoint |
| `jwks.js` | `jwks.js` | JWKS endpoint |
| `logout.js` | `logout.js` | OIDC logout |
| `vc_issuance_0.js` | `vc_issuance_0.js` | SD-JWT VC issuance step 0, the use-case chooser (`vc-issuance-0.html`) |
| `vc_issuance_1.js` | `vc_issuance_1.js` | SD-JWT VC issuance step 1 (`vc-issuance-1.html`) |
| `vc_issuance_2.js` | `vc_issuance_2.js` | SD-JWT VC issuance step 2 (`vc-issuance-2.html`) |
| `vc_issuance_3.js` | `vc_issuance_3.js` | SD-JWT VC issuance step 3 (`vc-issuance-3.html`) |
| `vc_issuance_4.js` | `vc_issuance_4.js` | SD-JWT VC issuance step 4, the credential refresh (`vc-issuance-4.html`) |
| `vc_presentation_0.js` | `vc_presentation_0.js` | SD-JWT VC presentation step 0, the flow chooser (`vc-presentation-0.html`) |
| `vc_presentation_1.js` | `vc_presentation_1.js` | SD-JWT VC presentation step 1, the verifier's request (`vc-presentation-1.html`) |
| `vc_presentation_2.js` | `vc_presentation_2.js` | SD-JWT VC presentation step 2, choose and present (`vc-presentation-2.html`) |
| `vc_presentation_3.js` | `vc_presentation_3.js` | SD-JWT VC presentation step 3, the verdict (`vc-presentation-3.html`) |
| `did_tools.js` | `did_tools.js` | DID Tools, a general-purpose DID verifier (`did-tools.html`) |
| `wsfed_request.js` | `wsfed_request.js` | WS-Federation sign-in/sign-out request builder (`wsfed_request.html`) — **renamed from `wsfed_tools` on 2026-08-09**, to match `saml_request.html` |
| `wsfed_response.js` | `wsfed_response.js` | WS-Federation response debugger (`wsfed_response.html`) |
| `webauthn_lab.js` | `webauthn_lab.js` | WebAuthn Lab — this origin as the relying party (`webauthn.html`) |
| `webauthn_analyzer.js` | `webauthn_analyzer.js` | WebAuthn Analyzer — decode artifacts from anywhere (`webauthn_analyzer.html`) |
| `kerberos_delegation.js` | `kerberos_delegation.js` | Kerberos delegation — S4U2Self, S4U2Proxy (classic and resource-based), forwarding and renewal (`kerberos_delegation.html`) |
| `pki.js` | `pki.js` | Certificate Authority &amp; X.509 Tools (`pki.html`) — the Root/Intermediate/Issuing hierarchy, the X.509v3 extension editor, and the api's TLS / mutual-TLS test. It is the only page here with **no in-browser option for its network call**, and `tests/pki_page.js` asserts that, because a browser cannot choose a client certificate, cannot be given a truststore and cannot read a handshake |
| `encryption_tools.js` | `encryption_tools.js` | Encryption / Decryption (`encryption_tools.html`) — nine panes, one per mechanism. The Digital Signature page's sibling: same grid, same key-pair sub-sections, same stylesheet, encryption instead of signatures. It is **DOM only** — every byte of cryptography is in a module with none, which is what lets `tests/crypto_engines.js` assert the RFCs' own vectors without a browser. See `docs/encryption.md` |
| `spnego.js` | `spnego.js` | SPNEGO — a Kerberos ticket in an HTTP header (`spnego.html`). The sixth Kerberos page; **its bundle name does not begin with `kerberos`**, which is why `client/build.js` stages `common/krb5` off an explicit list rather than a prefix test |
| `ldap.js` | `ldap.js` | LDAP Protocol Debugger (`ldap.html`). The only page here whose protocol runs ENTIRELY in the api — RFC 4511 is BER over a TCP socket — so it requires no codec of its own and stages nothing; what it needs is `ldap_history.js` and the api behind it |
| `scim.js` | `scim.js` | SCIM 2.0 Provisioning Debugger (`scim.html`) — one endpoint at a time, plus a scenario harness that creates, modifies and deprovisions whole populations. Its opposite number to `ldap.js` in one respect that matters: RFC 7644 is ordinary HTTPS with a JSON body, so this page calls a SCIM server **directly from the browser** and is on the static deployments. Almost all of it is in `scim_client.js` and `scim_scenarios.js`, which have no DOM |

The browserify build runs inside Docker. There is no local build script — to rebuild bundles you must use Docker.

**One file in `client/src` is not a module and must not be treated as one: `coverage_beacon.js`.** It ships browser coverage back to the client server, and the coverage build gets it there by *appending* it to each finished bundle — `cat src/coverage_beacon.js >> public/js/${src_name}.js`, in the `COVERAGE` block of `client/Dockerfile`. So it is the only file here that browserify and envify never see: in the browser it is raw script text where **`require` and `process` do not exist**, and its `log` is a console-backed shim written *inside* its IIFE (at the top it would be new globals in the page's own scope). A `require("bunyan")` added to it by the 2026-08-14 style sweep therefore threw before `setInterval()` ever ran, which collected **zero** frontend coverage and failed the 12 tests that assert a clean browser console — none of which named the beacon. Only `./run-coverage.sh` appends it, so no ordinary run can reproduce that; `appendedBeaconNeedsNoModuleSystem()` in `tests/jwk_pem_encoding.js` reads the file instead. Anything it needs from configuration has to be a literal in it.

**A new page has to be registered in THREE places**, and they are not near each other: the `BUNDLES` array in `client/build.js` (which the static deployments use), a `RUN browserify` line in `client/Dockerfile` (which the container image uses), and the `for entry in "src:standalone"` loop in that same Dockerfile's `COVERAGE` block (which only `./run-coverage.sh` executes). Miss the second and the deployed static site is perfectly fine while the containerized page's `<script>` 404s — so the failure appears only in the suite, and only as a page that does nothing.

**Miss the third and NOTHING fails.** The page builds, ships, works and passes every test it has; it simply reports no coverage, and the only symptom is a number in a report nobody diffs. Until 2026-08-22 seven bundles were missing from it — all six Kerberos pages and `pki` — and the Dockerfile had carried a comment *saying* six of them were, for months, because a comment is not a check. `coverageListCoversEveryBundle()` in `tests/jwk_pem_encoding.js` now compares all three lists on every ordinary run (not just under `./run-coverage.sh` — that is the point, since the plain launchers never execute that block) and fails naming the bundle and the list. It also catches a `--standalone` name that disagrees between the two builds: that global is what every inline `onclick` calls, so a mismatch makes every click on the page a `ReferenceError` under coverage and nowhere else.

**A page that cannot work without the api needs a third:** `client/static_site.js`, whose `EXCLUDED_PAGES` / `EXCLUDED_ASSETS` the static build reads to leave that page, its bundle and its own stylesheet out of `dist/`, and whose `CARD_MARKER` it uses to grey the landing card. Kerberos is the only member today, and all five of its pages are in it. The Dockerfile entry stays: the exclusion is static-only, and `tests/static_site_exclusions.js` fails if the container build loses one. The entries stay in `BUNDLES` too, filtered rather than deleted, so a static-only omission does not read as a page nobody ever registered. Note what the module does *not* do — it fails loudly rather than quietly: `fs.rmSync` on an absent path succeeds, so a rename would turn the whole exclusion into a no-op, and the build throws instead. Same for a card whose marker stopped matching, and for any surviving page that links to a dropped one.

### Keeping `elliptic` out of the bundles

`elliptic` carries GHSA-848j-6mx2-7j84 — ECDSA signing computes `k`'s byte length wrongly when `k` has leading zeros, truncating it, which produces a faulty signature and can expose the private key to anyone holding both the faulty and a correct signature for the same input. **There is no patched version and there is not going to be one**: the advisory's range is `<=6.6.1` and 6.6.1 is the latest release, so `npm audit fix` cannot resolve it and an override has nothing to point at. The only remedy is to stop requiring it.

It was reaching **five** bundles — `oauth2_oidc_1.js`, `oauth2_oidc_2.js`, `jwks.js`, `token_detail.js`, `userinfo.js` — never because a page wanted ECDSA, but because browserify substitutes a bare `require('crypto')` with the whole **crypto-browserify** shim, and that shim contains browserify-sign and create-ecdh, and those contain `elliptic`. Four requires were pulling it, and three of the four were **dead code**:

| Require | Where | Replaced by |
|---|---|---|
| `jsonwebtoken` | `jwks.js`, `userinfo.js` — imported, never called | deleted |
| `jsonwebtoken` | `token_detail.js`, for one `jwt.decode(t, {complete:true})` | a local `decodeJWT()`, since decoding a JWT is base64url and no cryptography. Signature *verification* on that page was already Web Crypto's |
| `@fidm/x509` (plus `pem-file`, `@peculiar/asn1-pkcs8`) | `jwks.js` — the only references were two commented-out lines | deleted |
| `jwk-to-pem` | `jwks.js`, genuinely used | `./jwk_pem` |
| `require('crypto')` | `oauth2_oidc_1.js`, for the PKCE `code_challenge` SHA-256 | **`create-hash`** — the very module crypto-browserify uses for `createHash`, so it is the same implementation and the same digest without the ECDSA half. Web Crypto is *not* the answer here: `crypto.subtle.digest` is async while `setPKCEValues()` calls this synchronously, and `crypto.subtle` does not exist at all on the containerized suite's `http://client:3000` origin |

Three rules follow, and `tests/jwk_pem_encoding.js` enforces all three:

* **No file in `client/src` may require `jwk-to-pem`, `jsonwebtoken`, `@fidm/x509`, or a bare `crypto`.** Any one of them silently re-adds `elliptic` to whatever bundle it lands in. This is the only check that would catch a *new* page doing it. (Comments discussing these requires are fine — that is where the reasoning lives — so only real code lines count.)
* Those packages are **removed from `client/package.json`**, so a future `npm install` cannot quietly restore the option.
* `browserify` and the minifiers are **devDependencies**, not dependencies. They are build tools that ship to nobody, and moving them is what makes `npm audit --omit=dev` report **0 vulnerabilities** — the full `npm audit` still shows the four `elliptic` lows, correctly, because the *build tool* still contains it. Both images install with plain `npm ci`, which includes devDependencies, so the build is unaffected. Note `client/Dockerfile` uses `npm ci`, so **`client/package-lock.json` must stay in sync with `package.json`** or the image build fails outright — unlike `api/`, which uses `npm install` and whose lock is knowingly stale.

What remains is `browserify`'s own copy, on disk in the built image. It is not reachable from any page, and removing it needs the runtime image to prune devDependencies (a multi-stage build, or `npm prune --omit=dev` after the bundling steps plus dropping the global `npm install -g browserify`) — not done.

### No BigInt literals in `client/src`

`0n`, `8n`, `0xffn` — write `BigInt(0)` instead, and hoist it: `var _B0 = BigInt(0), _B8 = BigInt(8), _B255 = BigInt(255);`. Both `digital_signature.js` and `bbs.js` do, and both learned it the same way.

The reason is the toolchain, not the language. **browserify runs the `envify` transform over every file in a bundle that references `process.env`, and `envify` parses with an `esprima` build old enough to predate BigInt literals** — it rejects one outright as `Line NNN: Unexpected token ILLEGAL`. `insert-module-globals` lexes the same files for the same reason.

What makes this expensive is *when* it fires. A literal is completely harmless until the file it sits in acquires a `process.env` reference — and then the build fails with a **syntax error against a file the change never touched**, reported in the bundle of whichever page happens to require it, however many requires away that is. `bbs.js` carried three of them for months while nothing in it mentioned `process`; adding a `log.debug` logger (whose level comes from `CONFIG_FILE`) took out `browserify src/vc_presentation_2.js` at `Error: Line 104: Unexpected token ILLEGAL`, with nothing in the message naming `bbs.js`, BigInt, or the logger.

So the rule is "none in `client/src`", not "none in a file that mentions `process.env`": the trigger is not a property of the file, so a rule conditioned on it is one that passes until the day it matters. `bigIntLiteralsStayOutOfTheBundles()` in `tests/jwk_pem_encoding.js` enforces it, beside the `elliptic` check and for the same reason — it is the only thing that catches it without a full image build, and it is mutation-tested against putting `0xffn` back.

**If you hit `Unexpected token ILLEGAL` from a browserify step, do not start with the file the step names.** Run `esprima` over every file in that bundle's require graph; the offender is usually a module the entry point reaches indirectly.

