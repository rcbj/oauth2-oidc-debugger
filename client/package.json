{
  "name": "docker_web_server",
  "version": "0.9.0",
  "description": "idptools client project",
  "author": "Robert C. Broeckelmann Jr. <robert@iyasec.io>",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node build.js"
  },
  "comments": {
    "devDependencies": "Build-only tools: the bundler, its transform, and the three minifiers. They are separated from dependencies because they are not part of anything that runs or ships to a browser, and because browserify carries elliptic (GHSA-848j-6mx2-7j84, for which no patched version exists) through crypto-browserify. Nothing bundled requires elliptic any more \u2014 tests/jwk_pem.js is what holds that \u2014 so the advisory now describes a build tool only, and `npm audit --omit=dev` says so. Both images install with plain `npm ci`, which includes these, so the build is unaffected."
  },
  "dependencies": {
    "@noble/curves": "1.4.0",
    "@noble/hashes": "1.4.0",
    "@noble/post-quantum": "^0.4.1",
    "asn1js": "^3.0.10",
    "bunyan": "latest",
    "create-hash": "^1.2.0",
    "dompurify": "latest",
    "express": "^4.22.2",
    "express-logging": "latest",
    "jquery": "latest",
    "jwks": "latest",
    "logops": "latest",
    "node-forge": "^1.3.1",
    "pkijs": "^3.4.0"
  },
  "devDependencies": {
    "browserify": "^17.0.1",
    "clean-css-cli": "^5.6.3",
    "envify": "latest",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.44.0"
  },
  "overrides": {
    "brace-expansion@1": "^1.1.17",
    "brace-expansion@2": "^2.1.3"
  }
}
